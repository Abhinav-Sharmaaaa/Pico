[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pico"
version = "0.2.0"
description = "Lightweight cross-platform AI terminal assistant with TUI, workflows, and web search"
readme = "README.md"
license = {text = "MIT"}
authors = [{name = "Abhinav Sharma", email = "abhinav@example.com"}]
keywords = ["ai", "terminal", "tui", "assistant", "openrouter", "workflow"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
]
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
# GUI dependencies for the desktop pet
gui = ["PyQt5>=5.15.2", "PySide6>=6.5.0"]
dev = [
    "pytest>=7.0",
    "pytest-asyncio>=0.21",
    "black>=23.0",
    "ruff>=0.1",
]

[project.scripts]
# Main CLI entry
pico = "pico.cli:main"
# Desktop pet executable entry
pico-pet = "pico.desktop.pet_cli:main"
pico-web = "pico.webui:main"

[project.urls]
Homepage = "https://github.com/Abhinav-Sharmaaaa/LinAI"
Repository = "https://github.com/Abhinav-Sharmaaaa/LinAI"
Issues = "https://github.com/Abhinav-Sharmaaaa/LinAI/issues"

[tool.setuptools.packages.find]
where = ["."]
include = ["pico*"]

[tool.black]
line-length = 100
target-version = ['py310']

[tool.ruff]
line-length = 100
target-version = "py310"